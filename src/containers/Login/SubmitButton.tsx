"use client";

import { Button } from "@/components/ui/button";

function SubmitButton() {
  return (
    <Button
      type="submit"
      variant="blue"
      className="w-full "
    >
      Login
    </Button>
  );
}

export default SubmitButton;
